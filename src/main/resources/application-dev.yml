spring:
  config:
    import: "optional:file:.env[.properties]"

  application:
    name: money
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI}
      database: ${SPRING_DATASOURCE_DATABASE}
      auto-index-creation: true
  main:
    log-startup-info: true

  mail:
    username: ${SUPPORT_EMAIL}
    password: ${APP_PASSWORD}
    host: smtp.gmail.com
    port: 587
    protocol: smtp
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.debug: false

security:
  jwt:
    secret-key: ${JWT_SECRET_KEY}
    expiration: 3600000

ai:
  xai:
    api-key: ${XAI_API_KEY:}
    model: ${XAI_MODEL:grok-4-fast-non-reasoning}
    responses-url: ${XAI_RESPONSES_URL:https://api.x.ai/v1/responses}
    max-output-tokens: ${AI_MAX_OUTPUT_TOKENS:220}
    max-expenses: ${AI_MAX_EXPENSES:40}

server:
  port: 8080

logging:
  level:
    root: INFO
    com.tracker.expenses.money: DEBUG
    org.springframework.web: INFO
    org.springframework.data.mongodb: INFO
    com.mongodb: INFO

#logging:
#  level:
#    root:
#logging:
#  level:
#    org:
#      springframework:
#        mail=DEBUG:
springdoc:
  show-login-endpoint: true
